import 'package:flutter/material.dart';

class CreateDashboard extends StatelessWidget {
  const CreateDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Container(
          color: Colors.red,
        ),
      ),
    );
  }
}
